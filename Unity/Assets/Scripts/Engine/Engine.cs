using UnityEngine;
using System.Collections;

public class Engine : MonoBehaviour
{

    public GameState applyGameState(VehicleAction playerAction, VehicleAction AIAction, GameState currentGameState)
    {

        return default(GameState);
    }

}
